spring:
  application:
    name: springhw
  datasource:
    url: jdbc:mysql://localhost:3306/likelion_guestbook
    username: likelion_hw
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true